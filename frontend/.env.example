# API Configuration
# For development, point to the backend directly
VITE_API_URL=http://localhost:8080/api/pdf

# For production/Docker, the nginx proxy handles /api (comment out VITE_API_URL or set to /api/pdf)
# VITE_API_URL=/api/pdf

# Development
VITE_DEV_PORT=5173
